for (let i = 1000; i <= 2000; i++) {
    if (i % 3 === 0) {
        console.log(`The number ${i} is divisible by 3`);
    } else {
        console.log(`The number ${i} is not divisible by 3`);
    }
}